# Call-core Node 1 Configuration
# Dev Testnet - 3 Node Setup

# Node identity
node_name = "call-dev-node-1"

# P2P network settings
# Listen on all interfaces, port 51235
listen_address = "0.0.0.0:51235"

# Bootstrap peers - connect to other nodes in the devnet
# Using Docker network IPs (172.21.0.x subnet)
peers = [
    "172.21.0.3:51236",
    "172.21.0.4:51237",
    "172.21.0.5:51238",
]

# Data directory for blockchain storage
data_dir = "/data/node1"

# Validator configuration
# This node is a validator with a deterministic seed for testing
validation_seed = "sEdTLQ75P2X9VBbNqGihzrNWGtE7d6NHPmgSG8q5d8fM7YjHcXK"

# Peer connection limits
max_peers = 50
target_peers = 10

# RPC server configuration
rpc_enabled = true
rpc_bind_address = "0.0.0.0"
rpc_port = 5005

# Enable admin RPC methods (for devnet testing)
rpc_admin_enabled = true

# Logging
log_level = "debug"
